import { JSONClient } from './json-client'

describe('Integration test up against real dbus service', () => {
  test('Ping command (no body)', async () => {
    //const jsonClient = new JSONClient('socketpath.sock')
  })
})
